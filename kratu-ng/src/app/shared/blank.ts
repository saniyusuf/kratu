import { Component } from '@angular/core';

/** An empty stop the router passes through when a screen must be re-entered (a sample session started from the chooser). */
@Component({ selector: 'app-blank', template: '' })
export class BlankScreen {}
